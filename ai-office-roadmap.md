# AI 协同真实人类办公系统：9 个当前任务路线

## 总目标

把现有多智能体 coding agent 演进为一个 AI 为主体、真实人类参与的 C 端办公系统。

这个系统的核心不是“让人更方便地调用 AI”，而是让 Agent 成为团队协作里的默认行动者：

- 人类提出目标、判断价值、承担责任、处理现实世界动作。
- Agent 理解主人的角色、能力、偏好和边界。
- Agent 之间先自主沟通、拆解、执行、汇总。
- 只有当问题需要人类判断、现实动作、审美判断、正式承诺或授权时，Agent 才请求人类。
- 系统持续记录协作过程，让 Agent 在长期使用中更理解人、更会协作。

## 设计原则

这份路线按以下原则重写：

1. 相信模型能力，不为了短期模型不稳定而暴露过多工程参数。
2. 保留简单、真实、抽象的操作语义。
3. 不把 runtime 细节暴露为产品概念。
4. 不把未来可能需要的东西提前塞进 MVP。
5. 尽量让人类社会已有的协作直觉自然映射到系统里。
6. 先定义干净的世界观，再考虑工程落点。

## 最终系统的基本世界观

系统里只有几个核心概念：

```text
人
Agent
工作
空间
消息
工具
记忆
```

它们之间的关系是：

- 人拥有 Agent。
- Agent 代表人参与工作。
- 工作发生在空间里。
- 协作通过消息完成。
- 工具让 Agent 触达数字世界和真实世界。
- 记忆让 Agent 在长期中理解人和团队。

---

# 任务 1：Agent Runtime 与执行环境彻底解耦

## 核心思想

Agent 的职责只应该是规划、决策、协作和表达意图，不应该直接接触 Docker 沙箱。

我们要建立的是三层结构：

```text
Agent Runtime — Middleware 网关 — Docker 沙箱
```

其中：

- Agent Runtime 负责模型推理、工作拆解、协作消息、记忆、时间线和工具意图生成。
- Middleware 网关负责接收工具意图、做鉴权和校验、做注入防范、做命令翻译、做超时控制，并把请求转发给执行环境。
- Docker 沙箱负责真正执行命令、脚本、文件操作和外部任务，只回传结果。

这样一来，Agent 可以运行在本地电脑，也可以运行在远程服务上；真正的执行环境可以在另一台机器上，通过我们的通道来控制。

用户看到的仍然是工作推进、协作过程和产物，而不是底层容器细节。

## 要做的事

把现有 `codex-rs` 里与思考、编排、协作和状态有关的部分保留为 Agent Runtime。

它继续负责：

- 模型调用。
- Agent turn 执行。
- Agent 间通信。
- 协作消息与等待。
- 工作摘要与时间线。
- 工具意图的生成与记录。

但它不再直接承担 Docker 执行职责。

真正的执行过程交给 Middleware 网关和沙箱后端。

Middleware 网关要提供一个统一的 HTTP 接口，让 Agent 以 OpenAI Function Calling 标准发起工具调用。这个接口负责：

- 鉴权。
- 参数校验。
- 注入防范。
- 命令翻译。
- 超时控制。
- 执行路由。

然后由 Middleware 再交给 Docker 沙箱执行。

沙箱必须保持执行隔离：

- 非 root 用户运行。
- 禁用网络。
- 限制内存和 CPU。
- 只回传 stdout、stderr 和退出码。

在它上面定义更自然的产品世界：

- 人。
- Agent。
- 工作。
- 空间。
- 消息。
- 记忆。

runtime 的 `Thread/Turn/EventMsg` 不直接成为产品 API，而是成为产品世界背后的执行痕迹。

工具执行也不直接暴露给 Agent，而是通过 Middleware 网关这一层统一收口。

## 预期效果

做完这一步后，系统不会被 coding agent 的内部概念绑架。

Agent、Middleware 和 Docker 沙箱之间有清晰边界：

- Agent 只负责想和说。
- Middleware 只负责接单、校验、转译和路由。
- 沙箱只负责执行。

这意味着：

- 本地 Agent 也可以通过同一通道控制远端执行环境。
- 以后如果执行后端更换为别的沙箱实现，Agent 侧不需要重写。
- 用户看到的是“工作正在推进”，而不是“thread 正在执行”。

Agent 对外表现为一个长期存在的协作者，而不是一次性的模型会话。

---

# 任务 2：建立最小协作对象和统一消息

## 核心思想

协作世界第一版只需要两种对象：Agent 和人。

不提前引入频道、外部 bot、复杂 scope、复杂消息路由。

因为真实协作的基本形式已经足够简单：

```text
Agent 对 Agent 说话
Agent 对人说话
人对 Agent 说话
```

## 要做的事

把协作接口收敛为：

```text
call(target_id, content, message_id, need_reply=false, reply_to_message_id?)
wait(target_id)
```

其中：

- `target_id` 可以是 Agent，也可以是人。
- `message_id` 用来唯一标记本次消息。
- `reply_to_message_id` 用来回复上一条消息，必须保留现有 Swarm 语义。
- `need_reply` 用来显式登记“需要对方回复”的闭环。
- Agent 不需要传 `target_kind`。
- Agent 不需要传 `conversation_scope`。
- Agent 不需要知道消息挂在哪个 task 下面。
- 当前上下文由系统从运行环境中隐式获得。

消息统一为一种东西：

```text
Message
  - message_id
  - from
  - to
  - content
  - need_reply
  - reply_to_message_id?
```

这里要复用当前 `codex-rs` 里已经跑通的 call 语义：

- 新请求使用新的 `message_id`。
- 回复时使用新的 `message_id`，并把 `reply_to_message_id` 指向原始请求。
- `need_reply=true` 表示系统需要记录一个等待回复的 obligation。

人类打断、纠正、拒绝、补充，本质上也都是消息。

## 复用现有 Swarm 提示词

新项目里只要涉及 `call`、`wait`、Swarm 协作、回复闭环，就必须复用现有 `codex-rs` 的 相关工具 提示词设计。

重点复用：

- `core/templates/collaboration_mode/swarm_main.md`
- `core/templates/collaboration_mode/swarm_main_complex.md`
- `core/templates/collaboration_mode/swarm_sub.md`
- `core/templates/collaboration_mode/swarm_sub_worker.md`
- `core/templates/collaboration_mode/swarm_sub_explorer.md`
- `core/templates/collaboration_mode/swarm_sub_verifier.md`
- 对应的 complex 版本

这些提示词已经包含高质量的 `call` / `wait` 使用纪律和示例，例如：

- 什么时候应该 `call`。
- 什么时候应该 `wait`。
- `message_id` 如何保持唯一。
- 回复时如何使用 `reply_to_message_id`。
- 不要空等。
- 不要 ping 式打扰。
- 如何形成通信闭环。

因此，新产品不应该重新发明一套协作提示词，而应该把这些 Swarm prompt 作为基础协作规范，再在外层加入人类主人和产品上下文。由于新产品里很多 Agent 是长期存在的，不一定每次都从 spawn 开始，所以原提示词里强依赖 spawn 流程的内容可以裁剪，保留 call、wait、message_id、reply_to_message_id 和协作纪律。

## 当前上下文从哪里来

上下文不是 Agent 每次要填的参数，而是系统自动知道的环境。

来源包括：

- 人从哪个工作页面启动 Agent。
- Agent 被哪条消息唤醒。
- Agent wait 后从哪里恢复。
- 没有明确工作时，系统创建一个临时工作。

所以 Agent 只需要在当前工作里自然发消息。

## 预期效果

Agent 的协作行为接近人类：

```text
call("infra_agent", "帮我估算 GPU 成本", need_reply=true)
wait("infra_agent")
```

或者：

```text
call("user_zhangsan", "这个预算可以申请吗？", need_reply=true)
wait("user_zhangsan")
```

系统自己决定消息如何投递、如何展示、如何恢复上下文。

---

# 任务 3：统一等待与人类介入

## 核心思想

等待不应该分裂成很多工具。

Agent 只需要：

```text
wait(target_id)
```

系统根据 `target_id` 判断是在等 Agent，还是在等人。

## 要做的事

当 Agent 等 Agent：

- 对方收到“有人正在等你”的提示。
- 当前 Agent 默认等待 7 分钟。
- 超时后 Agent 自己决定继续等、换人、自己推进或请求人类。

当 Agent 等人：

- 人的 inbox 里出现待处理请求。
- 当前 Agent 不空转。
- 当前 Agent 持久挂起。
- 没有默认超时。
- 人回复、拒绝、纠正、取消，都会以消息形式唤醒 Agent。
- 其他相关消息也可以唤醒 Agent。

人类介入不需要特别事件。

人类对 Agent 说一句话，就是介入。

## 主人画像更新

不要在每次回复后立刻修改主人画像。

每天做一次定时复盘：

- 昨天 Agent 找了主人几次。
- 主人完成了哪些。
- 主人拒绝了哪些。
- 哪些请求打扰了主人。
- 哪些判断体现了主人的强能力。
- Agent 是否误判了主人的边界。

复盘后生成主人画像更新。

这更稳定，也更像真实长期关系的形成。

## 预期效果

人类不会被卷入低质量聊天。

人类看到的是明确请求：

- 为什么找我。
- 需要我判断什么。
- 我不回复会阻塞什么。
- Agent 已经做了哪些前置工作。

Agent 等人时不会消耗模型 turn，而是安静挂起，等消息到来后继续推进。

---

# 任务 4：定义 Agent 的工作空间

## 核心思想

Agent 需要一个“办公室”。

办公室不是暴露给 Agent 的 Docker 概念，而是 Agent 工作时自然拥有的空间。

它包括：

- Agent 自己的私有草稿区。
- 团队共享区。
- 当前工作的上下文资料。
- Agent 能使用的工具和文件。

这个办公室必须有一个非常清晰的默认规则：

> 只要工作和团队协作、共同任务、共享产物、其他人可能受益的信息有关，就应该进入公共空间完成。私有空间只用于主人上传材料的接收和完全个人化、非协作性的临时处理。

也就是说，公共空间是协作的默认场所；私有空间是少数例外场所。

## 要做的事

定义空间语义，而不是先定义容器细节。

空间里有三类东西：

```text
私有材料
共享材料
工作产物
```

私有材料用于 Agent 思考、实验、草稿。

共享材料用于团队协作、交付、沉淀。

工作产物是某次工作最终留下来的文件、报告、代码、分析、结论。

进一步定义私有空间和公共空间的边界：

```text
私有空间
- 接收主人上传的文件。
- 临时处理主人上传的个人材料。
- 完成和其他人、团队任务、共享产物都没有关系的独立工作。

公共空间
- 承载强团队协作关联的任务。
- 承载多人共同推进的工作。
- 承载可能被其他 Agent 或人类复用的材料、结论和产物。
- 承载正式任务过程中的分析、决策、交付物和共享上下文。
```

典型例子：

- 主人上传一个 PDF，文件首先进入该主人的 Agent 私有空间。
- Agent 只是帮主人抽取 PDF 里的目录、摘要、关键句，而且这个工作和团队任务无关，可以留在私有空间。
- 如果 PDF 内容会影响某个团队项目、某个共享任务、某个其他 Agent 的工作判断，就应该把相关摘要或产物迁移到公共空间。
- 如果 Agent 正在做强团队协作相关任务，即使中间过程还不成熟，也应该在公共空间完成，避免信息孤岛。

底层可以用 Docker、volume、对象存储、索引系统实现，但 Agent 不需要知道。

Agent 只知道：

- 我可以读哪些材料。
- 我可以写哪些草稿。
- 我可以把哪些东西提交为共享产物。

每个 Agent 的系统提示词都应该写入这条空间使用规则。

建议放入 Agent system prompt 的核心文本：

```text
你拥有私有工作空间和公共工作空间。

私有工作空间主要用于两类事情：
1. 接收你的主人上传的文件。
2. 处理和其他人、团队协作、共享任务都没有关系的个人化临时工作。

公共工作空间是团队协作的默认场所。

如果一项工作和团队任务、多人协作、共享产物、其他 Agent 或其他人可能受益的信息有关，你必须优先在公共工作空间中完成。

如果你先在私有空间中处理了某份材料，但后来发现它和公共任务有关，你应该把相关摘要、结论或产物迁移到公共空间，而不是让信息停留在私有空间。

不要把强团队协作相关任务留在私有空间中完成。
```

## 预期效果

Agent 工作会像真实人类在办公室里工作：

- 有自己的桌面。
- 有团队共享资料柜。
- 有当前项目文件夹。
- 有最终交付物。

用户能看到 Agent 产出的东西，而不是只能看到聊天。

---

# 任务 5：把已有工具接入 Middleware 网关

## 核心思想

工具本身已经在现有 coding agent 里定义好了，不需要重新发明。

现有系统已经有：

- shell / unified exec。
- 文件读写。
- apply patch。
- MCP。
- dynamic tools。
- approval / sandbox。
- tool summary。

所以这个任务不是“重新生成工具”，也不是再定义一套工具系统。

这个任务要做的是：

> 把已有工具抽象成 Agent 能力，把真正执行动作收口到 Middleware 网关。

## 要做的事

保留现有工具目录和能力定义，但改变它们的执行位置。

Agent 侧只产出工具意图：

```text
Function Call / Tool Intent
```

Middleware 网关负责把这个意图转成真正的执行请求。

可以把执行链理解成：

```text
Agent Runtime -> Middleware Gateway -> Sandbox / Connector
```

其中：

- Agent Runtime 负责生成工具调用意图。
- Middleware Gateway 负责鉴权、校验、注入防范、命令翻译和超时控制。
- Sandbox / Connector 负责真正执行。

产品层只做一层更自然的解释：

```text
工具 = Agent 能力
工具网关 = Agent 能力的统一入口
工具调用记录 = Agent 工作痕迹
```

例如：

- shell / exec 对产品层来说是“执行代码和脚本的能力”。
- 文件读写对产品层来说是“处理工作材料的能力”。
- MCP 对产品层来说是“连接外部工具的能力”。
- approval 对产品层来说是“哪些动作需要主人确认”。
- summary 对产品层来说是“Agent 当前在做什么的简短记录”。

Agent 不需要知道背后具体工具名，更不需要直接接触 Docker。

用户也不需要看到工具 schema。

用户只需要知道：

```text
这个 Agent 能读资料。
这个 Agent 能写草稿。
这个 Agent 能跑代码。
这个 Agent 不能直接对外发送正式消息。
这个 Agent 做高风险操作前要问我。
```

## 预期效果

这个任务完成后，现有工具不会被替换，只是执行边界被重新定义。

变化只是：

- 工具在产品世界里变成能力。
- 工具网关成为统一入口。
- 工具权限变成 Middleware 要执行的安全策略。
- 工具 summary 继续作为 Agent 状态和时间线的原料。

这样既复用现有工具系统，又保证 Agent runtime 和 Docker 执行环境彻底解耦。

---

# 任务 6：把工作过程变成自然时间线

## 核心思想

时间线不是手写的，也不是让用户读 runtime log。

时间线应该由两层组成：

```text
底层：真实事件流
上层：每累计 15 个 Agent step 触发一次总结智能体
```

底层保证真实、可追溯。

上层保证自然、可读、像人在汇报工作。

## 要做的事

### 1. 记录真实事件流

系统持续记录：

- Agent 收到目标。
- Agent 调用了哪个 Agent。
- Agent 等待谁。
- 人类回复了什么。
- Agent 使用了什么工具。
- 工具 summary 是什么。
- Agent 产出了什么文件或结论。

这些事件不一定全部直接展示给用户。

它们是时间线的事实基础。

### 2. 每累计 15 个 Agent step，调用总结智能体

这里的 step 指的是 Agent 的一次运行循环。

这个计数是全局累计的，不区分单智能体还是多智能体。

例如：

- 3 个 Agent 各运行 5 个 loop。
- 这 15 个 loop 加起来，就触发一次总结智能体。

触发时，系统调用一个专门的总结智能体，把当前真实事件流总结成自然时间线。

总结智能体的输入不是全量聊天，而是经过筛选的事件、summary 列表和当前工作状态。

总结智能体输出用户能读懂的工作进展：

```text
Leader Agent 已拆解研究目标。
Infra Agent 正在估算 GPU 成本。
Product Agent 已整理 3 个用户场景。
当前阻塞点是预算确认，正在等待张三回复。
报告草稿已生成第一版。
```

### 3. 保留事实与总结的关系

自然时间线里的每一句总结，都应该能追溯到底层事件。

这样可以避免模型总结变成幻觉。

## 预期效果

用户看到的是自然工作流：

- 当前进展是什么。
- 谁在做什么。
- 哪些事情完成了。
- 哪些事情卡住了。
- 哪些人类需要介入。
- 产物在哪里。

系统内部仍然保留完整事件流，方便追溯和训练。

---

# 任务 7：基于 Summary 列表发现高价值协作机会

## 核心思想

调度层的价值不是做低级提醒。

阻塞、等待回复、谁没有回消息，这些事情每个 Agent 自己已经能通过 inbox、wait、summary 感知到，不需要外部调度层频繁介入。

调度层真正有价值的地方，是发现 Agent 自己未必能发现的协同增益：

> 两个 Agent 或两个人的工作看似独立，但如果把他们拉到一起交流，可能产生 1+1 大于 2 的价值。

例如：

- 两个研究 Agent 分别在看不同论文，但它们的方法可能互补。
- 一个 infra Agent 正在解决部署问题，另一个算法 Agent 的模型结构选择会显著影响部署成本。
- 产品 Agent 发现的用户痛点，可能正好能启发算法 Agent 的实验方向。
- 两个人类员工都在接近的问题上积累了经验，但彼此还不知道。

调度层要做的是识别这种“潜在高收益交流”，而不是频繁提醒简单事项。

它使用的主要输入，是每累计 15 个 Agent step 之后由总结智能体产出的 summary 批次，再结合当前工作方向、产物、画像和已有交流记录做判断。

## 要做的事

调度层读取：

- 最近一次或最近几次总结智能体生成的 summary 列表。
- 每个 Agent 的当前工作方向。
- 最近产出的文件、报告、实验结论。
- 人类主人画像中的能力标签。
- Agent 之间已经发生过的交流。
- 当前工作里尚未连接起来的知识线索。

其中最核心的是 summary 列表。

summary 是 Agent 对自己当前工作的自然压缩，适合用来判断不同 Agent 之间是否存在潜在互补关系。

调度层基于 summary 判断：

- 两个 Agent 是否在研究相邻问题。
- 一个 Agent 的发现是否可能帮助另一个 Agent。
- 两个 Agent 是否从不同角度接近同一个关键问题。
- 某个人类的经验是否可能启发某个 Agent。
- 某个产物是否应该被另一个 Agent 看到。
- 当前任务是否存在“没有人意识到的跨域连接”。

## 调度层系统提示词原则

调度层的 system prompt 应该明确限制它不要做低价值调度。

提示词核心应该是：

```text
你是协同增益发现器，不是普通待办提醒器。

你的目标不是提醒 Agent 回复消息、不是催促阻塞、不是重复 Agent 自己已经能看见的状态。

你的目标是发现潜在的高价值交流机会：
- 两个 Agent 的工作可能互补。
- 某个 Agent 的发现可能启发另一个 Agent。
- 某个人类的经验可能让当前工作产生跃迁。
- 两条看似无关的工作线索可能连接成更好的方案。

只有当你判断一次交流可能产生明显增益时，才建议拉起沟通。

不要频繁调度。
不要因为普通等待、普通阻塞、普通未回复就调度。
不要替 Agent 做它们自己能做的提醒。
如果没有明显 1+1>2 的交流价值，保持沉默。

如果需要调度，优先给出“谁和谁交流、为什么、可能带来什么增益”，而不是给出简单催促。
```

## 调度层输出

调度层输出不应该是“催促某人”。

它应该输出类似：

```text
建议让 Agent A 和 Agent B 交流。
原因：A 正在研究的模型压缩方案可能显著降低 B 正在处理的部署成本。
预期增益：两者交流可能同时优化算法效果和 infra 成本。
```

或者：

```text
建议让 Product Agent 把用户痛点摘要发给 Algorithm Agent。
原因：Algorithm Agent 当前实验目标过于抽象，而 Product Agent 刚发现了一个高频用户场景。
预期增益：实验方向可能更贴近真实需求。
```

## 预期效果

调度层会更像一个高水平团队协调者，而不是提醒机器人。

它不频繁出现。

它出现时，应该让 Agent 或人觉得：

> 这个连接我自己可能没想到，但确实有价值。

这样调度层才会成为系统的增益来源，而不是噪音来源。

调度层自身的判定依据应该围绕这种协同增益展开：

```text
summary 序列 + 产物线索 + 人类能力画像 -> 高价值交流机会
```

---

# 任务 8：跑通多个最小真实场景

## 核心思想

MVP 不追求功能完整，而是先跑通多个真实场景，让系统自然覆盖核心工作形态。

最重要的问题是：

> Agent 能不能真的代表人参与团队协作，并在需要人类时自然请求人类？

## 要做的事

设计多组最小真实场景，而不是只用一个 research team 场景。

### 场景 1：Research Team 调研

1. Leader 给自己的 Agent 一个研究目标。
2. Leader Agent 理解目标并拆解工作。
3. Leader Agent 找算法 Agent、infra Agent、产品 Agent 协作。
4. 各 Agent 自己查资料、写脚本、分析。
5. Infra Agent 遇到预算判断，请求人类确认。
6. 人类回复后任务继续。
7. 最终形成共享报告和自然时间线。

这一场景要自然产出：

- Agent-to-Agent 协作。
- Agent-to-Human 请求。
- 人类回复后恢复执行。
- 共享产物沉淀。

### 场景 2：产品需求评审

1. 产品 Agent 收到一个模糊需求。
2. 产品 Agent 找设计 Agent 判断交互风险。
3. 产品 Agent 找工程 Agent 判断开发成本。
4. 设计 Agent 请求人类判断审美方向。
5. 工程 Agent 给出实现风险 summary。
6. 产品 Agent 汇总成 PRD 草稿。

这一场景要自然产出：

- 多 Agent 从不同视角补全模糊目标。
- 人类介入高主观判断。
- 时间线能解释决策来源。

### 场景 3：代码问题排查

1. 工程 Agent 收到一个线上 bug 描述。
2. 工程 Agent 查代码和日志。
3. 复现 Agent 还原问题。
4. 工程 Agent 修改方案草稿。
5. 如果涉及高风险改动，请求人类确认。
6. 最终生成修复说明和修复结论。

这一场景要自然产出：

- 复用现有 coding agent 工具能力。
- summary 列表反映排查状态。
- 工具调用进入自然时间线。

### 场景 4：会议准备

1. 人类让 Agent 准备一次讨论会。
2. Agent 收集背景资料。
3. Agent 找相关人的 Agent 补充各自负责部分。
4. Agent 整理议程和待决策问题。
5. 对关键议题请求人类提前确认。

这一场景要自然产出：

- Agent 代表人收集信息。
- Agent 间协作减少人类沟通成本。
- 输出物不是聊天，而是会议材料。

### 场景 5：每日复盘

1. 每天固定时间触发复盘。
2. Agent 读取当天消息、summary、工具产物、人类回复。
3. Agent 总结今天完成了什么、卡住了什么、打扰人类是否合理。
4. Agent 生成主人画像更新建议。
5. 低风险偏好自动更新，高风险能力判断留待确认。

这一场景要自然产出：

- 主人画像不是实时乱改，而是定时总结。
- summary 和时间线能支撑长期记忆。
- Agent 对人类能力边界逐步变准。

## 预期效果

这些场景共同覆盖了系统最关键的几种工作形态：

- Agent 主动协作。
- Agent 请求人类。
- 人类低噪音介入。
- Agent 继续执行。
- 产物沉淀。
- summary 和时间线支撑调度与长期记忆。


---

# 任务 9：建立人和 Agent 的绑定关系

## 核心思想

如果每个人都有一个 Agent，那么系统必须先解决一个最基础的问题：

> Agent 怎么知道自己是谁的 Agent？又怎么知道自己的主人能做什么、负责什么、喜欢什么、不适合做什么？

这不是附属功能，而是整个产品能不能成立的入口。

Agent 和人的关系不能只靠系统 prompt 写死。第一版可以预设，但真实使用时需要让 Agent 通过对话理解主人。

## 要做的事

### 1. 先做公司内部 6 人试点

先在公司内部实验。

团队规模暂定 6 人：

- 1 个 CEO。
- 5 个员工。

每个人有自己的账号和密码。

每个人登录同一个网站，但进入后绑定的是自己的 Agent。

网站通过当前服务器上的一个端口转发出来，作为内部试点入口。

第一版目标不是做复杂企业权限，而是让每个人都能完成：

```text
登录 -> 进入自己的页面 -> 和自己的 Agent 建立联系 -> Agent 记录主人画像
```

### 2. 预先定义主人标签，方便初期启动

为了让系统先跑起来，可以先手动给 6 个人配置初始标签。

例如：

```text
CEO
  - 负责方向判断
  - 负责资源决策
  - 负责最终优先级
  - 不适合被频繁打扰实现细节

员工 A
  - 擅长算法
  - 可以判断模型方案
  - 可以做实验设计

员工 B
  - 擅长 infra
  - 可以判断部署、算力、成本

员工 C
  - 擅长产品
  - 可以判断用户场景和需求优先级

员工 D
  - 擅长工程实现
  - 可以判断代码改动和技术风险

员工 E
  - 擅长运营或设计
  - 可以判断表达、审美、外部沟通
```

这些标签不是最终真相，只是启动系统的种子。

它们用于启动系统的初始判断：

- Agent 是否知道该找谁。
- Agent 是否知道什么问题应该找主人。
- Agent 是否能避免把问题派给不合适的人。

### 3. 用小访谈建立真实主人画像

真实使用时，每个人第一次登录后，自己的 Agent 会发起一个小访谈。

这个访谈不是普通问卷，而是 Agent 和人的自然对话。

Agent 需要逐步理解：

- 你的工作内容是什么。
- 你负责哪些决策。
- 你擅长什么。
- 你不希望被什么事情打扰。
- 哪些事情必须问你。
- 哪些事情可以让 Agent 自己做。
- 你喜欢怎样的汇报方式。
- 你通常什么时候方便处理 Agent 请求。

访谈结束后，Agent 生成一份主人画像草稿。

例如：

```text
主人画像
  - 角色：AI infra 工程师
  - 擅长：训练稳定性、集群部署、成本估算
  - 可被请求：GPU 预算判断、部署方案评审、故障排查
  - 不应打扰：普通资料搜索、简单文档整理、低风险代码格式调整
  - 偏好：先给结论，再给细节
  - 响应习惯：下午更容易处理请求
```

### 4. Agent 通过长期互动更新画像

初始画像不是一次性完成。

后续通过每日复盘持续更新：

- 今天 Agent 找主人是否合理。
- 主人拒绝了什么。
- 主人高质量回答了什么。
- 主人纠正了 Agent 哪些判断。
- Agent 是否应该减少某类打扰。

这样 Agent 对人的理解会逐步变准。

### 5. 人和 Agent 的登录关系

第一版需要一个非常简单的账号系统。

核心关系是：

```text
UserAccount -> HumanProfile -> AgentProfile
```

每个人登录后，只看到：

- 自己的 Agent。
- 自己的待办。
- 自己参与的工作。
- 自己 Agent 产生的总结和画像。

Agent 之间可以协作，但人类入口必须区分账号。

这样才能保证：

- Agent 找的是正确的人。
- 人类回复进入正确 Agent 的上下文。
- 每个人的画像和权限不会混在一起。

## 预期效果

完成这个任务后，系统不再只是“几个 Agent 在互相说话”。

它会变成：

- 每个真实员工都有自己的 Agent。
- 每个 Agent 知道自己代表谁。
- Agent 知道主人初始能力边界。
- Agent 能通过小访谈学习主人。
- 人类可以通过登录页面和自己的 Agent 建立关系。
- 后续所有 Agent-to-Human 的请求都有明确目标。

这一步是内部试点真正能跑起来的入口。
